// Objeto Math

let resultado;

// resultado = Math.PI;
// resultado = Math.round(2.3);
// resultado = Math.ceil(2.1); // Cell siempre redondea hacia arriba
// resultado = Math.floor(2.1); // Cell siempre redondea hacia abajo
// resultado = Math.sqrt(144); // Raíz cuadrada
// resultado = Math.abs(-200); // Combierte en positivo
// resultado = Math.min(1, 2, 8, 14); // Presenta el mínimo
// resultado = Math.max(3, 5, 86); // Presenta el máximo
// resultado = Math.random();
resultado = Math.floor(Math.random() * 30); // Usamos dos funciones para conseguir la salida de datos

console.log(resultado);